<%= form_for(@b_corpu) do |f| %>
  <% if @b_corpu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@b_corpu.errors.count, "error") %> prohibited this b_corpu from being saved:</h2>

      <ul>
      <% @b_corpu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :end_date_build %><br>
    <%= f.date_select :end_date_build %>
  </div>
  <div class="field">
    <%= f.label :b_queues_id %><br>
    <%= f.text_field :b_queues_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
