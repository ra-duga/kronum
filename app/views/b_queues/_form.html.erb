<%= form_for(@b_queue) do |f| %>
  <% if @b_queue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@b_queue.errors.count, "error") %> prohibited this b_queue from being saved:</h2>

      <ul>
      <% @b_queue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :end_date_build %><br>
    <%= f.date_select :end_date_build %>
  </div>
  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.text_field :building_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
