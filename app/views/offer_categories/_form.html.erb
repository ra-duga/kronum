<%= form_for(@offer_category) do |f| %>
  <% if @offer_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer_category.errors.count, "error") %> prohibited this offer_category from being saved:</h2>

      <ul>
      <% @offer_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :offer_id %><br>
    <%= f.text_field :offer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
